/* API do jeste -> jestjs.io */

describe('Generate Unique ID', () => {                               //Descrição do teste. Pode ser qualquer nome. Normalmente o nome da função 
    it('should generate an unique ID ', () => {                      //Este should generate an unique ID espera receber 2+2 e a resposta seja 4
        expect(2 + 2).toBe(5);
    });
});